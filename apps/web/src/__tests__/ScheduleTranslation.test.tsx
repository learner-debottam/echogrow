import { describe, it, expect } from 'vitest';
// import SchedulePage from '../../pages/schedule';

describe('SchedulePage Translation', () => {
  it('shows translated caption (placeholder)', async () => {
    expect(true).toBe(true);
  });
});
