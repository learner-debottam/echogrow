import { describe, it } from 'vitest';

describe('Auth API Integration', () => {
  it('should sign up, login, and fetch profile (integration)', async () => {
    // TODO: Implement integration test with Supabase test instance
  });
});
