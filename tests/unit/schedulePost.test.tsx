import { describe, it, expect } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import SchedulePage from '../../apps/web/pages/schedule';

describe('Schedule Post Page', () => {
  it('renders form and calendar (placeholder)', () => {
    expect(true).toBe(true);
  });

  it('shows skeleton loaders during fetch (placeholder)', () => {
    expect(true).toBe(true);
  });
});
