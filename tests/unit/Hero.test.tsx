import { describe, it, expect } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import Hero from '../../apps/web/components/Hero';

describe('Hero', () => {
  it('renders CTA buttons (placeholder)', () => {
    expect(true).toBe(true);
  });
});
