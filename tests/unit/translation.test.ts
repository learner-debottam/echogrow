import { describe, it, expect } from 'vitest';
// import { translateText } from '../../packages/ai-flows/translation';

describe('AI Translation', () => {
  it('translates caption (placeholder)', async () => {
    expect(true).toBe(true);
  });
});
